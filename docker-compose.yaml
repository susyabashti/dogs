services:
  postgres:
    image: postgres:17
    container_name: postgres_db
    restart: unless-stopped

    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-appdb}
      PGDATA: /var/lib/postgresql/data/pgdata

    # Expose PostgreSQL externally
    ports:
      - "${POSTGRES_PORT:-5432}:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-appdb}",
        ]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s

  # c_server:
  #   build:
  #     context: ./c_server
  #     dockerfile: Dockerfile
  #   container_name: restapi
  #   restart: unless-stopped

  #   depends_on:
  #     postgres:
  #       condition: service_healthy

  #   environment:
  #     DB_HOST: postgres
  #     DB_PORT: 5432
  #     DB_USER: ${POSTGRES_USER:-postgres}
  #     DB_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
  #     DB_NAME: ${POSTGRES_DB:-appdb}

  #   # Expose your C service externally
  #   ports:
  #     - "${C_SERVER_PORT:-8080}:8080"

  # Optionally add later once your C server has an HTTP health endpoint:
  # healthcheck:
  #   test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
  #   interval: 5s
  #   timeout: 2s
  #   retries: 5
  #   start_period: 5s

volumes:
  postgres_data:
